# Session Log — 2026-06-04

## What happened

Replaced Python microservice (sentence-transformers + FAISS) with MERN-native Ollama + application-level cosine similarity.

### Phase 1 — Ollama infrastructure (COMPLETED)
- Reverted prior Atlas Vector Search schema changes (those required a paid Atlas tier)
- `backend/src/ai/schemas/faq-embeddings.schema.ts` — new `FaqEmbedding` schema: `faqId`, `title`, `body`, `embedding` (384-dim float array)
- `backend/src/ai/embeddings.service.ts` — `EmbeddingsService` with `OllamaProvider` (HTTP to local Ollama) and `MockProvider` (deterministic hash-seeded pseudo-vectors, dev/test only)
- `backend/src/ai/faq-embeddings.service.ts` — `FaqEmbeddingsService`: `upsert()`, `rebuildAll()` (batched 16), `removeEmbedding()`, `findBestMatch()` (cosine similarity)
- `.env.example` updated with `EMBEDDING_PROVIDER=ollama|mock`, `OLLAMA_URL`, `OLLAMA_EMBEDDING_MODEL`, `AI_CONFIDENCE_THRESHOLD`
- `AiService` + `AiMatcherService` rewritten — no HTTP to Python
- `AdminService.rebuildIndex()` now calls `FaqEmbeddingsService.rebuildAll()` directly
- `manual_checklist.md` written — Ollama install, model pull, env config, rebuild-index steps

### Phase 2 — FAQ auto-indexing (COMPLETED)
- `FaqsModule` imports `AiModule` → `FaqEmbeddingsService` available in `FaqsService`
- `FaqsService.create()` → `upsert()` (fire-and-forget, non-blocking)
- `FaqsService.update()` → `upsert()` with updated title/body (fire-and-forget)
- `FaqsService.archive()` → `removeEmbedding()` (removes from AI match index)
- `FaqEmbeddingsService` gained `removeEmbedding()` method

### Phase 3 — Question embedding (COMPLETED)
- `question.schema.ts` — added `questionEmbedding?: number[]` field
- `QuestionsService.create()` — injects `EmbeddingsService`, generates embedding for title+body, persists `questionEmbedding` to the saved doc (fire-and-forget)
- Enables future "similar questions" lookups (not yet built)

### Context update (COMPLETED)
- `CONTEXT.md` fully updated with new Ollama vector search architecture, env vars, embedding lifecycles
- `MEMORY.md` updated

## Outstanding
- Issue 3 (`aiMatchFaqId` not saved on create path) — confirmed ALREADY FIXED (controller Shape 2 captures FAQ ID, passes to `create()`)
- Issue 17 — E2E tests need in-memory MongoDB fixture
- 3 minor frontend issues (#21, #25, #22 won't-fix)
- Superadmin pages not yet implemented
- Socket.IO real-time vote updates (Phase 2)
- `questionEmbedding` not yet used anywhere (future feature)

## TypeScript
`tsc --noEmit` ✅ exit 0 (backend)